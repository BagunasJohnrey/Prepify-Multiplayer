# ── Frontend configuration ──
# Copy this file to `client/.env` and adjust if your server runs elsewhere.

# Base URL of the API (note: must end with /api)
VITE_API_URL=http://localhost:3000/api

# Socket.IO endpoint (must be the server origin, NOT including /socket.io)
VITE_SOCKET_URL=http://localhost:3000
